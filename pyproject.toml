[project]
name = "python-practice-section-files"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kumar nallana", email = "sasikumarnallana956@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
python-practice-section-files = "python_practice_section_files:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.uv.workspace]
members = [
    "python_advance/env_project",
]
